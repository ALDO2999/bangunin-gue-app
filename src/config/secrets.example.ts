/**
 * Template for local secrets. Copy this file to `secrets.ts` and fill in your
 * own keys. `secrets.ts` is gitignored so real keys never get committed.
 *
 *   cp src/config/secrets.example.ts src/config/secrets.ts
 */
export const secrets = {
  // Free LocationIQ token (https://locationiq.com). Format: pk.xxxxxxxx
  locationIqKey: '',
};
